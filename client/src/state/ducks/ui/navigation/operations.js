import actions from '@State/ducks/ui/navigation/actions'

const {
  toggleMobileMenu,
  toggleLocationModal,
  toggleLoginModal,
  setFixedState,
} = actions

export default {
  toggleMobileMenu,
  toggleLocationModal,
  toggleLoginModal,
  setFixedState,
}
