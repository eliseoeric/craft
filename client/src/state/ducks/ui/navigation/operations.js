import actions from '@State/ducks/ui/navigation/actions'

const {
  toggleMobileMenu,
  setActiveTeamMemberIndex,
  toggleLoginModal,
  setFixedState,
} = actions

export default {
  toggleMobileMenu,
  setActiveTeamMemberIndex,
  toggleLoginModal,
  setFixedState,
}
